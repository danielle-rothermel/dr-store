Read `.defs/terms.toml` for the shared vocabulary and `.defs/contracts.toml`
for the binding behavioral rules before changing this repository.

Activate the tracked hook once with
`git config core.hooksPath scripts/git-hooks`. `scripts/pre-check.sh` is the
canonical developer check.

See user level instructions: ~/.codex/AGENTS.md
